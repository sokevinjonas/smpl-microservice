pour voir les logs en continu : sudo docker logs -f smpl-microservice

Redémarrer le conteneur pour appliquer mon correctif (le fichier est monté, donc pas besoin de tout reconstruire, juste redémarrer) : docker-compose restart smpl-microservice

Cela va recréer l'image:  docker-compose up --build